<?xml version="1.0"?>
<ruleset name="WPChecksumVerifier">
	<description>Coding standards for WP Checksum Verifier.</description>

	<file>.</file>
	<exclude-pattern>/vendor/*</exclude-pattern>
	<exclude-pattern>/languages/*</exclude-pattern>
	<exclude-pattern>/tests/*</exclude-pattern>
	<exclude-pattern>/lib/*</exclude-pattern>

	<arg value="sp"/>
	<arg name="basepath" value="."/>
	<arg name="colors"/>
	<arg name="extensions" value="php"/>

	<config name="minimum_supported_wp_version" value="6.8"/>
	<config name="testVersion" value="7.4-"/>

	<rule ref="WordPress-Extra">
		<exclude name="WordPress.Files.FileName"/>
		<!--
			例外メッセージはブラウザへの出力ではなく開発者・ログ向けのため、
			HTMLエスケープの対象ではない。検証エンジン(WPCV_Verifier等)は
			動的な内容を含む例外メッセージを多数投げる設計のため、行ごとの
			phpcs:ignoreを積み上げるよりここで除外する.
		-->
		<exclude name="WordPress.Security.EscapeOutput.ExceptionNotEscaped"/>
	</rule>

	<rule ref="WordPress-Docs"/>

	<rule ref="PHPCompatibilityWP"/>

	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="wpcv"/>
				<element value="WPCV"/>
			</property>
		</properties>
	</rule>
</ruleset>
