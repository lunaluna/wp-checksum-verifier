# このプラグインと wp-maintenance-audit-reporter (WPMAR) の関係(必読・絶対の前提)

## 背景

wp-checksum-verifier は wp-maintenance-audit-reporter(以下 WPMAR)をベースとして開発している。
最終的には、WPMAR 側で先行実装されていた機能のうち、このプラグインで実装し直した部分を
WPMAR から切り出し、**WPMAR と wp-checksum-verifier の2つのプラグインが連携して動作する**
構成に分割することを予定している。

## 実装上の絶対ルール

- **wp-checksum-verifier の実装は、WPMAR の実装とコンフリクトしない形を常に前提とすること。**
  - テーブル名・オプション名・cron hook名・REST namespace・DB schema version の保存先など、
    WPMAR と共存したときに衝突しうる命名・保存先を選ばない。
  - WPMAR が既に採用しているパターン(例: `sweep_stale_running()` のようなアクセス時
    ハートビート掃除方式、`base_prefix` を使ったマルチサイト installation-level の
    テーブル設計など)を踏襲できる場合は、独自のパターンを新たに作らず踏襲する
    (将来の分割・連携を見据えて設計の一貫性を保つため)。
- **WPMAR の実装への変更がどうしても必要になった場合は、その旨を明示すること。**
  - 「wp-checksum-verifier 側だけの変更では完結せず、WPMAR 側にも同等のパッチを
    当てる必要がある」ということをユーザーに申告してから進める。
  - 申告なしに WPMAR 側の実装を暗黙の前提として壊す・矛盾させる変更をしない。

## 参照

- WPMAR の実体は `test-armfu.local` サイト内の wp-content にある
  (詳細は auto memory の `wpmar-repository-location.md` を参照)。
- 過去に WPMAR との機能重複・分割方針を検討した資料が
  `~/.claude/plans/wpmar-overlap-analysis.md` にある(参考情報。最新の方針は
  このファイルの内容を優先すること)。
